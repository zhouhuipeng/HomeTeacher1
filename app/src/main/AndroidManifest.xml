<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asus.hometeacher">

    <uses-permission android:name="android.permission.SEND_SMS" />

    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <!--允许联网 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!--获取GSM（2g）、WCDMA（联通3g）等网络状态的信息  -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <!--获取wifi网络状态的信息 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!--保持CPU 运转，屏幕和键盘灯有可能是关闭的,用于文件上传和下载 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!--获取sd卡写的权限，用于文件上传和下载-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!--允许读取手机状态 用于创建BmobInstallation-->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- sd卡存储-->
    <!--摄像头-->
    <uses-permission android:name="android.permission.CAMERA" />
    <!--录音-->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!--通知震动-->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>

                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Registor"
            android:windowSoftInputMode="adjustNothing"></activity>
        <activity android:name=".login"
            android:windowSoftInputMode="adjustNothing"></activity>
        <activity android:name=".Mainpager"
            android:windowSoftInputMode="adjustNothing"></activity>
        <activity android:name=".Teacher_Perfectinformation"
            android:windowSoftInputMode="adjustNothing"></activity>
        <activity android:name=".Student_Perfectinformation"
            android:windowSoftInputMode="adjustNothing"></activity>
        <activity android:name=".User_message"
            android:windowSoftInputMode="adjustNothing"></activity>
        <activity android:name=".Send_Conversation"></activity>
        <activity android:name=".Change_student"
            android:windowSoftInputMode="adjustNothing"></activity>
        <activity android:name=".Change_teacher"
            android:windowSoftInputMode="adjustNothing"></activity>
        <activity android:name=".My_teacher"
            android:windowSoftInputMode="adjustNothing"></activity>
        <activity android:name=".My_student"
        android:windowSoftInputMode="adjustNothing"></activity>
        <activity android:name=".Change_score"
            android:windowSoftInputMode="adjustNothing"></activity>
    </application>

</manifest>